import React, { Component } from 'react';
import './ConfirmTab.css';

const ConfirmTab = ({form, children}) => {
  return (
    <main className="confirm-template">
      <div>
      <button>Confirm!</button>

      </div>
      
    </main>
  );
};

export default ConfirmTab;